# EduCase — Вопросы к кафедре (questions_for_department)

> Сюда сведены **все места, где нужны решения/данные**, которые нельзя восстановить из памяти
> ассистента. Это рабочий список: закрываем по пунктам, и проект перестаёт стоять на догадках.
> Часть вопросов — то, что я решал сам при сборке каркаса (помечено 🛠).

## 1. Содержательная фактура (документы и оценивание)

- [ ] **Приложение 22 (оценка СЭО):** точные **4 уровня** и **числовые пороги** между ними.
- [ ] **Приложение 23:** точные **формулы расчёта заболеваемости** (переменные, округление).
- [ ] **Акт расследования вспышки:** полный перечень **11+ полей** и их типы.
- [ ] По каждому документу (ДМ4/форма 23/акт ГСЭН/донесение командира/план/приказ): **поля**,
      **ожидаемые значения**, **правило сравнения** (точное/числовое/с допуском).
- [ ] **Документы-обманки:** какие именно идут в списки выбора на каждом этапе.
- [ ] **Оценивание:** что считается ошибкой, как из ошибок формируется **финальный отчёт**,
      нужны ли баллы/проценты или только разбор.

## 2. Модель данных

- [ ] Реальная схема `Case`/`Stage`/`Document` и вспомогательных сущностей (заменить скелет в
      DATA_MODEL.md).
- [ ] Поля **PatientCard**, **DocumentField**, **ObservationTimeline**.
- [ ] Структура **facility_scheme**, **action_icons**, **hotspots** осмотра помещений.
- [ ] Точная схема `manifest.json` внутри `.educase`/`.eduresult`: версия формата, id кейса,
      контрольная сумма, метаданные; раскладка ассетов и ссылки на них.
- [ ] Валидация при импорте: поведение при несовместимой версии / повреждённом архиве.

## 3. Архитектурные развилки (решал сам при сборке — нужно подтвердить) 🛠

- [ ] **Структура монорепо:** общий `educase_core` + `educase_constructor` + `educase_player`
      в одном репозитории, один `pyproject.toml`, два GUI-входа. Так ли задумано?
- [ ] **Alembic:** намеренно **не добавлен** (для десктопа со свежей БД миграции часто лишние).
      Нужны ли версии БД / миграции, или схема создаётся при первом запуске?
- [ ] **Где живёт кейс в Constructor:** локальный SQLite для авторской работы, или Constructor
      работает прямо с `.educase` без своей БД? (Влияет на объём ORM-слоя.)
- [ ] **Список и границы кастомных скиллов:** сверить мои 4 скилла с оригинальным skills_plan.md.

## 4. Нумерация и статусы ADR

- [ ] Подтвердить нумерацию ADR-001, 002, 004–008 (точно известен только ADR-003) и проставить
      статусы/даты в decisions.md.

## 5. Организационное

- [ ] **Разрешён ли облачный хостинг исходников** (GitHub) для материалов кафедры, или нужен
      приватный/локальный? Сейчас репозиторий создан как Public — проверить регламент.
- [ ] Источник **реальных шаблонов документов** (сканы/файлы приложений) для заполнения скиллов.
